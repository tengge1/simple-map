
// BaseEvent
class BaseEvent {

    constructor(map) {
        this.map = map;
    }

    handle(eventSystem) {
        // register mouse event or keyboard event here.
        // eg: eventSystem.register('mousedown', this.onMouseDown);
    }

}